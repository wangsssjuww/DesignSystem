import ImageGallery from '../componen/image';
import Layout from '../componen/layout'; // adjust the path as needed

const Page = () => {
    return (
        <Layout>
            <div>
                <ImageGallery/>
            </div>
        </Layout>
    );
};

export default Page;
